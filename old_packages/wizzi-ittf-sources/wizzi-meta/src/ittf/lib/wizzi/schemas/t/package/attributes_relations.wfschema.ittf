$group

	a kind
		restrict
			enum ittfsource
			enum kernel
			enum plugin
			enum demo
		default ittfsource
	a version
	a title
	a license
		default MIT
	a_tag( srcCwd, src-cwd )
	a_tag( gitCwd, git-cwd )

	r schema/s
	r generatedPackage/s
	
