base_test( util.types )

	$append imports
		var types = require('../../lib/util/types')

	$*
	it should objectify
		
		_ types.objectify
            @ item
            @ 'hello'
            @ 'hook'
			@ startItem
	*$

