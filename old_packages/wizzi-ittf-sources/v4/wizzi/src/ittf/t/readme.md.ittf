mdx

	h2 Implicit assumptions in this t-folder
	ul
		li check_* fragments are input argument validations
		li checked_call_* fragments are checks on the returned value of a function or method call
		li check_r_* and checked_call_r_* imply that the error is returned (inside sync functions),
			+ otherwise a callback is the default behaviour (inside async functions)