module
	kind jsfile

	var md = module.exports = {}
	
	set md.getSchemaDefinition
		function
			param schemaName
			if schemaName === 'tests'
				return
					{
						@ name 'tests'
			else
				return null

