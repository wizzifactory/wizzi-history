mdx

	$include readme_vars

	h2 jsWizzi

	h3 Reference
	p
		a Concept
			href ${url_concepts}#concept-3
		span &nbsp; | &nbsp;
		a Docs
			href ${url_docs}/jswizzi.html
		span &nbsp; | &nbsp;
		a Demo ittf source
			href ${url_demo_ittf_source}/jswizzi

	h3 Demo samples level-0

	table
		demo_step(1)
			p Using an `html` ittf document to show samples of jswizzi statements.
			p The `html` artifact here is not generated.
			p We build the mTree only, just to take a look to node transformations.