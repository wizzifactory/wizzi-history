demo_module( jswizzi )

	# 
		# DEMO: Using an `html` ittf document to show
		#       samples of jswizzi statements.
		# The `html` artifact here is not generated.
		# We build the mTree only, just to take a look to 
		# node transformations.
	+
	loadMTree( 1, basic.html.ittf )
