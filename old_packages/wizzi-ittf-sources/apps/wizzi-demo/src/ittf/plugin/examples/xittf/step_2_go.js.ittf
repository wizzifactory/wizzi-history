demo_module( plugin_examples, xittf, 2 )

	# 
		# DEMO: How to use the `xittf` wizzi model type to produce
		#       an ittf document of type 'css'
		#       using fragments, mixins and functions
		#       in a Sass manner.
	+
	loadMTree( 2, second.xittf.ittf, print:dump )
		loadModel( 2, second.xittf.ittf, save;print:json )
			loadModelAndGenerateArtifact( 2, second.xittf.ittf, xittf, document, save; print:artifact )