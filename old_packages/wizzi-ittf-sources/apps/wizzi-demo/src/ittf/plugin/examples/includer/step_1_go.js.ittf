demo_module( plugin_examples, includer, 1 )

	# 
		# DEMO: How to use the `includer` wizzi model type to 
		#       build a includer
	+
	loadMTree( 1, first.includer.ittf, print:dumpx )
		loadModel( 1, first.includer.ittf, save;print:json )
