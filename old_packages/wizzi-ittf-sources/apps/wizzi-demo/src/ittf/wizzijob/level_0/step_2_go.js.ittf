demo_module( wizzi_job_multiartifact, 0, 2 )
	# 
		# DEMO: how to execute a wizzi job to
		# generate a set of javascript modules
		# from a set of ittf documents of type `js` in the same folder
		# The `src` attribute of the `artifact` elements contains a `glob` pattern.
	+
	var testBaseFolder = __dirname
	+
	exec( simple, step_2.wfjob.ittf, 2 )
