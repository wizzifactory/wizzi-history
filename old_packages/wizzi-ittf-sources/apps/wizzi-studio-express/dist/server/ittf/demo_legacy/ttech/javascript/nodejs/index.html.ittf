html
	head
		css /static/lib/normalize.css
	body
		h1 Node JS

		h3 File system
		ul
			lia(rimraf - The UNIX command rm -rf for node. , https://github.com/isaacs/rimraf)
			lia(mkdirp - Like mkdir -p but in node.js!, https://github.com/substack/node-mkdirp )

