div
	$params formid, id, label, value
	class form-group
	label ${label}
		class col-sm-4
		@ for ${id}
	div
		class col-sm-8
		input
			class form-control
			@ placeholder ${label}
			@ required true
			@ type text
			@ id fd_${formid}_${id}
			@ name ${id}
			@ value ${value}

