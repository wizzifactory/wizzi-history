$group
	$params name, label, placeholder|@@null
	
	< Field
		@ name ${name}
		@ label ${label}
		@ component {renderField}
		@ type email
		$if placeholder != null
			@ placeholder ${placeholder}
		$hook		$hook