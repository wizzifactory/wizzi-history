$group

	@ getSchemaDocuments
		function
			param options
			param callback

			var packageName = options.packageName
			var schemaName = options.schemaName

			return
				_ callback
					@ null
					{
						@ status
							_ success
								@ 'Schema documents data fetched'

