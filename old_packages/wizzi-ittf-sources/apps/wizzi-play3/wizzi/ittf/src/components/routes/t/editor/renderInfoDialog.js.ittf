$group

	< WizziMTreeDebugInfo
		@ open {mTreeDebugInfoOpen}
		@ lastDebugInfoAction {lastDebugInfoAction}
		@ mTreeScript {mTreeScript}
		@ generatedArtifact {generatedArtifact}
		@ jobExecutionResult {jobExecutionResult}
		@ jobExecutionError {jobExecutionError}
		@ schemaGenerationResult {schemaGenerationResult}
		@ schemaGenerationError {schemaGenerationError}
		@ onClose {this.handleMTreeDebugInfoClose}
