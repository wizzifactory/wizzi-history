$group

	function isAttribute
		param name
		# log 'isAttribute', name
		return
			iif attributes.indexOf(name) > -1
				then true
				else false 

	var attributes
		[
			@ 'xclass/xsuper'
			@ 'method/xstatic'
			@ 'property/xstatic'
			@ 'prop/defaultValue'
