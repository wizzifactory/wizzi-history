$group

	#
	set md.createWizziFactory
		function
			param options
			param callback

			var store = options.store
			# create store

			var storeOptions = options.plugins

			var PluginsManager = require('./services/pluginsManager')
			var pluginsManager
