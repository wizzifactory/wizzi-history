$group
	
	function isWizziPackageSchema
		param name
		
		var ndx
			[
				@ 'nools'
				@ 'npmpackage'
				@ 'wftest'
				@ 'wfjob'
				@ 'wfpackage'
				@ 'wfschema'
			._ indexOf(name)
		return ndx > -1