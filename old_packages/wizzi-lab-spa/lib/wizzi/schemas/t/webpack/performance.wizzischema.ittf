$group
	e performance : webpackitem
		# https://webpack.js.org/configuration/performance/
		tag performance
		a hints 
			default false
			restrict
				enum false
				enum warning
				enum error
		a_tag( maxEntrypointSize, max-entry-point-size )
			type integer
		a_tag( maxAssetSize, max-asset-size )
			type integer
