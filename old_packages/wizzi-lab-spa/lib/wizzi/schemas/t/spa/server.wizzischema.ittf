$group
	e server : spaitem
		restrict
			enum webpack
			enum express
		a env
			default dev
			restrict
				enum dev
				enum production
		a port
			default 3000
		
		a contentBase
			tag content-base
		a_flag( historyApiFallback, history-api-fallback )
		
		a_flag( hot )
		
		a_flag( true )
		
		a_flag( inline )
		
		a_flag( progress )
		
		a host
		
		a port
