$group
	
	e_tag( directiveDef, directive, typeDef
		r comment/s
		r argumentDef/s
		r directiveLocation/s

	e_tag( directiveLocation, on

	e_tag( directive, %
		r argument/s
		r directiveLocation/s

	e_tag( isList, %list, directive
	
	e_tag( isRequired, %required, directive
	
	e_tag( isDeprecated, %deprecated, directive
	
	e_tag( skip, %skip, directive
	
	e_tag( include, %include, directive		
