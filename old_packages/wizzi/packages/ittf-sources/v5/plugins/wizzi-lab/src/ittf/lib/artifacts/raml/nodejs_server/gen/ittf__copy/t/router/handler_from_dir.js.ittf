$group

	var path = require('path')
	var fs = require('fs')


	var handlerCacheFromDir
        function 
            param dirOrDirs
            
			var handlerCache
                { 
            
			var dirs
                [ 
            
			if _.isArray(dirOrDirs)
                set dirs = dirOrDirs
            else 
                _ dirs.push(dirOrDirs)
            
			$include recursiveFileList
			
			_ _.each(dirs, recursiveFileList)
            
			return handlerCache
    


