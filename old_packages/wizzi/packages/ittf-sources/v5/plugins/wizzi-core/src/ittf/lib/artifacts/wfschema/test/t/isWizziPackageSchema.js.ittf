$group
	
	function isWizziPackageSchema
		param name
		
		var ndx
			[
				@ 'nools'
				@ 'npmpackage'
				@ 'wftest'
				@ 'wfjob'
				@ 'wfpackage'
				@ 'wizzischema'
			._ indexOf(name)
		#		@ 'js'
		return ndx > -1