$group

	a kind
		restrict
			enum ittfsource
			enum kernel
			enum plugin
			enum demo
		default ittfsource
	a version
	a title
	a license
		default MIT
	a_tag( fsBaseUri, fs-base-uri )
	a_tag( packageBaseUri, package-base-uri )
	a_tag( gitBaseUri, git-base-uri )

	r schema/s
	r generatedPackage/s
	
