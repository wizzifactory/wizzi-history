. form-group
	$params id, label, value|@@null, haserror:boolean|false, error|@@null
	id ${id}_group
	f_group( ${id}, ${label}, ${haserror}, ${error} )
		select
			class form-control
			id ${id}
			$hook