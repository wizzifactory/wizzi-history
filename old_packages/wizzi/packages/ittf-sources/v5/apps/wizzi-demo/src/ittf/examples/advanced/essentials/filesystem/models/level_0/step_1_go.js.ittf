demo_module( wizzimodel, 0, 1 )

	# 
		# Wizzi model
		p How to load an `html` wizzi model from a single ittf document.
		#
	+
	loadModel( 1, basic.html.ittf, print:model dashes
		if step_callback
			return
				_ step_callback(null)