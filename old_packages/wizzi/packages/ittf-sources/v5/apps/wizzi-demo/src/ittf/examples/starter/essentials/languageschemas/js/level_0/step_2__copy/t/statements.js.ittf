$group

	+
	# ========================================
	# STATEMENTS
	# ========================================
	+

	#
		# The 'export' element can export default and named functions (and objects).
	export var foo = Math.sqrt(2)
	export
		var foo = Math.sqrt(2)
	export
		var foo
			{
				@ name 'stefi'
	export
		function Hello
			string name
	
	export default
		var foo = Math.sqrt(2)
	export default
		{
			@ name 'stefi'
	export default
		function
			{ options
				string name
			callback



