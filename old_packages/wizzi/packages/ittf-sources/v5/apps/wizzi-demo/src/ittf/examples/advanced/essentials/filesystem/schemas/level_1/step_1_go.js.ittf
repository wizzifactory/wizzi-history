demo_module( wizzischema, 1, 1 )

	# 
		# Wizzi schema
		# How to define a free element with an implicit type in a wizzi schema.
	+
	exec_go( 1, xittf, first )
		if step_callback
			_ step_callback
				@ null