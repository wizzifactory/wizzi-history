md

	h1 JsWizzi examples
	p JsWizzi is the script language that you can use to transform an ittf document
		\b into a template.
	p 
		a Documentation
			href https://wizzifactory.github.io/docs/jswizzi.html

	h3 Demo samples level-0

	table
		demo_step(1)
			p This step uses an `html` ittf document to show samples of jswizzi statements.
			p The `html` artifact is not generated.
			p We simply build the mTree, just to take a look at the created nodes.
