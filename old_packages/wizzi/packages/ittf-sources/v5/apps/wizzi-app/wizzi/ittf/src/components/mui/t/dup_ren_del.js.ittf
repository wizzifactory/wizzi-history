$group

	{
		&&
			+ onDuplicate
			< Tooltip
				@ title Duplicate
				< Button
					@ className {classes.navLinkButton}
					@ onClick {onDuplicate}
					< DuplicateIcon
	{
		&&
			+ onRename
			< Tooltip
				@ title Rename
				< Button
					@ className {classes.navLinkButton}
					@ onClick {onRename}
					< RenameIcon
	{
		&&
			+ onDelete
			< Tooltip
				@ title Delete
				< Button
					@ className {classes.navLinkButton}
					@ onClick {onDelete}
					< DeleteIcon
