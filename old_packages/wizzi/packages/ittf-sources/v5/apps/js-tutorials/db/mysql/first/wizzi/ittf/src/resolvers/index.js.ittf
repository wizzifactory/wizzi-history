module
	kind es6
	
	const { extractFragmentReplacements } = require('prisma-binding')
	const { Query } = require('./Query')

	var resolvers
		{
			@ Query
	
	log 'resolvers 1', resolvers

	set module.exports = resolvers