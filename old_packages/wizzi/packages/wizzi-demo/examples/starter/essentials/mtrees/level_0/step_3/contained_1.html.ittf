layout( contained1 )

	$append left-sidebar
		lia( home, /home )
		lia( docs, /docs )
		lia( about, /about )

	$
		var sections = ['products', 'services', 'support']

	ul
		$foreach section in sections
			lia( ${section}, /${section} )

