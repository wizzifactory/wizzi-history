$group
	h3 Statements
	
	$
		var name = 'Derby', winner = null;
		if (name == 'Ascot') {
			winner = 'Ribot';
		} else if (name == 'Arc de triomphe') {
			winner = 'Varenne';
		} else if (name == 'Derby') {
			winner = 'Ribot';
		} else {
			winner = 'Varenne';
		}

	p If the race was ${name} the winner was ${winner}.
	br
	br

