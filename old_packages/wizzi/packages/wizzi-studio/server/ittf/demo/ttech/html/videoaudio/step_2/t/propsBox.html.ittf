$group
	
	section
		class controls
		id propsBox
                   
		h/control( size )
			h/input( image-width, small, image-width, ${opt.iframeW} )
			h/input( image-height, small, image-height, ${opt.iframeH} )
		
		h/control( figure )
			h/input( figcaption, xl, figcaption, '' )