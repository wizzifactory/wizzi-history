. form-group
	$params id, label, model|@@null, value|@@null
	id ${id}_group
	f_group( ${id}, ${label} )
	select
		class form-control input-sm
		id ${id}
		name ${id}
		$if model
			data-ng-model ${model}
		$hook
